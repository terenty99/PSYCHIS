import React from 'react';
import { TimeDisplay } from './TimeDisplay';

// Backwards-compatible export with timer functionality stripped out
export const ClockTimerWidget = () => {
  return <TimeDisplay />;
};
